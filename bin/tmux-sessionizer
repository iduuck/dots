#!/usr/bin/env bash

set -e

projects_found=$(ls -d /Users/nick/{.dotfiles,dev/**})

selected_project=`echo "$projects_found" | fzf`
session_name=`basename $selected_project | tr . _`

cd $selected_project
tat $session_name
