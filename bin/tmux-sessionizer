#!/usr/bin/env bash

# This shell script is used for indexing the projects in the ~/dev directory.
# It is used to cd into the project directory and start a tmux session.

set -e

cd $(ls -d /Users/nick/{.dotfiles,dev/**} | fzf)
